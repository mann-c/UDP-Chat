Execute the program on two different computers or two different terminals

Run program on command line, pass in three commands in order:
1. the port from which the message will be sent
2. the name of the computer you are chatting with
3. the port on which messages will be received

"!" is the sentinel value that signifies that a user wants to end the program. Enter "!" in either instance of the program to exit.